class Solution(object):
    def rotateString(self, s, goal):
        """
        :type s: str
        :type goal: str
        :rtype: bool
        """
        n = len(s)

        for _ in range(n):
            if s == goal:
                return True
            s = s[1:] + s[0]

        return s == goal
